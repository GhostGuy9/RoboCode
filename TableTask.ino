#include "bot_move.h"

void setup() {
  // put your setup code here, to run once:

}

void loop() {
  
  moveForward(6);
  turnLeft(1);
  moveForward(2);
  turnLeft(1);
  moveForward(2);
  turnLeft(1);
  moveForward(2);
  turnRight(1);
  moveForward(2);
  turnRight(1);
  moveForward(2);
  turnRight(1);
  moveForward(2);
  turnRight(1);
  moveForward(2);
  turnLeft(2);
  moveForward(6);
  
}
